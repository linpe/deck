import React from 'react';
import styles from './folder-placeholder.css';

const FolderPlaceholder = () => (
  <div className={styles.container}>
    <div className={styles.folder} />
  </div>
);

export default FolderPlaceholder;
